**What & why**

**How tested** (run `bash test/test.sh`; note any live Claude Code testing)
- [ ] `bash test/test.sh` passes
- [ ] No `continue:false` added (enforcement stays boundary-clean, never an abrupt mid-response kill)
- [ ] Zero new dependencies
